spring:
  application:
    name: demo

camunda:
  client:
    mode: self-managed
    auth:
      method: none
    grpc-address: http://localhost:26500
    security:
      plaintext: true

logging:
  level:
    io.camunda.client.job.poller: error
